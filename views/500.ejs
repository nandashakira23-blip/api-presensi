<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Server Error</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'dark': {
                            800: '#212529',
                            900: '#1a1d20',
                        },
                        'brown': {
                            300: '#d4bfa6',
                            400: '#c4a882',
                            600: '#a67c52',
                        }
                    }
                }
            }
        }
    </script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: #1a1d20;
        }
        .glitch {
            animation: glitch 2s infinite;
        }
        @keyframes glitch {
            0%, 100% { transform: translateX(0); }
            20% { transform: translateX(-5px); }
            40% { transform: translateX(5px); }
            60% { transform: translateX(-5px); }
            80% { transform: translateX(5px); }
        }
        .fade-in {
            animation: fadeIn 1s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .glow {
            box-shadow: 0 0 20px rgba(166, 124, 82, 0.3);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4">
    <div class="text-center max-w-2xl mx-auto">
        <div class="mb-8 text-8xl">
            <i class="fas fa-server text-brown-400 glitch"></i>
        </div>
        
        <h1 class="text-9xl font-bold text-brown-600 mb-4 glitch">500</h1>
        
        <h2 class="text-3xl font-bold text-white mb-4 fade-in">Terjadi Kesalahan Server</h2>
        
        <p class="text-brown-300 text-lg mb-8 fade-in">
            Maaf, server mengalami masalah. Tim kami sedang memperbaikinya.
        </p>

        <div class="flex flex-wrap gap-4 justify-center mb-8 fade-in">
            <a href="/admin/dashboard" class="px-6 py-3 bg-brown-600 hover:bg-brown-400 text-white rounded-lg font-medium transition-all duration-200 glow">
                <i class="fas fa-home mr-2"></i> Kembali ke Dashboard
            </a>
            <a href="javascript:location.reload()" class="px-6 py-3 bg-dark-800 hover:bg-dark-700 text-brown-300 border border-brown-600/30 rounded-lg font-medium transition-all duration-200">
                <i class="fas fa-sync-alt mr-2"></i> Muat Ulang Halaman
            </a>
        </div>

        <div class="bg-dark-800 border border-brown-600/30 rounded-xl p-6 text-left fade-in glow">
            <h3 class="text-lg font-bold text-white mb-4">
                <i class="fas fa-tools text-brown-400 mr-2"></i> Apa yang bisa Anda lakukan?
            </h3>
            <ul class="space-y-2 text-brown-300">
                <li><i class="fas fa-check text-brown-400 mr-2"></i> Tunggu beberapa saat dan coba lagi</li>
                <li><i class="fas fa-check text-brown-400 mr-2"></i> Muat ulang halaman ini</li>
                <li><i class="fas fa-check text-brown-400 mr-2"></i> Kembali ke halaman utama</li>
                <li><i class="fas fa-check text-brown-400 mr-2"></i> Hubungi administrator jika masalah berlanjut</li>
            </ul>
        </div>

        <% if (typeof error !== 'undefined' && process.env.NODE_ENV === 'development') { %>
        <div class="bg-dark-800 border border-brown-600/30 rounded-xl p-6 text-left mt-4 fade-in">
            <h3 class="text-lg font-bold text-white mb-4">
                <i class="fas fa-bug text-brown-400 mr-2"></i> Debug Info (Development Only):
            </h3>
            <pre class="bg-black/50 p-4 rounded-lg overflow-x-auto text-sm text-brown-300"><%= error.stack || error.message || 'No error details available' %></pre>
        </div>
        <% } %>
    </div>

    <script>
        document.addEventListener('keydown', function(e) {
            if (e.key === 'r' || e.key === 'R') location.reload();
            if (e.key === 'h' || e.key === 'H') window.location.href = '/admin/dashboard';
        });
    </script>
</body>
</html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - Server Error</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .container {
            text-align: center;
            color: white;
            padding: 20px;
            position: relative;
            z-index: 10;
        }

        .error-code {
            font-size: 150px;
            font-weight: bold;
            margin-bottom: 20px;
            animation: glitch 2s infinite;
            text-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .error-title {
            font-size: 36px;
            margin-bottom: 15px;
            animation: fadeInUp 1s ease-out;
        }

        .error-message {
            font-size: 18px;
            margin-bottom: 30px;
            opacity: 0.9;
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        .buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease-out 0.4s both;
        }

        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: white;
            color: #f5576c;
            font-weight: 600;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid white;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-3px);
        }

        .broken-robot {
            font-size: 80px;
            margin-bottom: 20px;
            animation: shake 2s infinite;
            display: inline-block;
        }

        .info-box {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            margin-top: 30px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
            animation: fadeInUp 1s ease-out 0.6s both;
        }

        .info-box h3 {
            margin-bottom: 10px;
            font-size: 18px;
        }

        .info-box ul {
            list-style: none;
            text-align: left;
        }

        .info-box li {
            padding: 8px 0;
            padding-left: 25px;
            position: relative;
        }

        .info-box li:before {
            content: "-";
            position: absolute;
            left: 0;
        }

        /* Animations */
        @keyframes glitch {
            0%, 100% {
                transform: translateX(0);
            }
            20% {
                transform: translateX(-5px);
            }
            40% {
                transform: translateX(5px);
            }
            60% {
                transform: translateX(-5px);
            }
            80% {
                transform: translateX(5px);
            }
        }

        @keyframes shake {
            0%, 100% {
                transform: rotate(0deg);
            }
            10%, 30%, 50%, 70%, 90% {
                transform: rotate(-5deg);
            }
            20%, 40%, 60%, 80% {
                transform: rotate(5deg);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .error-code {
                font-size: 100px;
            }

            .error-title {
                font-size: 28px;
            }

            .error-message {
                font-size: 16px;
            }

            .buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 200px;
            }
        }

        /* Circuit lines */
        .circuit {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
            opacity: 0.1;
        }

        .circuit-line {
            position: absolute;
            background: white;
            animation: circuitFlow 3s infinite;
        }

        @keyframes circuitFlow {
            0% {
                opacity: 0.1;
            }
            50% {
                opacity: 0.3;
            }
            100% {
                opacity: 0.1;
            }
        }
    </style>
</head>
<body>
    <!-- Circuit background -->
    <div class="circuit" id="circuit"></div>

    <!-- Main content -->
    <div class="container">
        <div class="broken-robot"></div>
        <div class="error-code">500</div>
        <h1 class="error-title">Oops! Terjadi Kesalahan Server</h1>
        <p class="error-message">
            Maaf, server mengalami masalah. Tim kami sedang memperbaikinya.
        </p>

        <div class="buttons">
            <a href="/admin/dashboard" class="btn btn-primary">
                Kembali ke Dashboard
            </a>
            <a href="javascript:location.reload()" class="btn btn-secondary">
                Muat Ulang Halaman
            </a>
        </div>

        <div class="info-box">
            <h3>Apa yang bisa Anda lakukan?</h3>
            <ul>
                <li>Tunggu beberapa saat dan coba lagi</li>
                <li>Muat ulang halaman ini</li>
                <li>Kembali ke halaman utama</li>
                <li>Hubungi administrator jika masalah berlanjut</li>
            </ul>
        </div>

        <% if (typeof error !== 'undefined' && process.env.NODE_ENV === 'development') { %>
        <div class="info-box" style="margin-top: 20px; text-align: left;">
            <h3>Debug Info (Development Only):</h3>
            <pre style="background: rgba(0,0,0,0.2); padding: 15px; border-radius: 8px; overflow-x: auto; font-size: 12px; text-align: left;">
<%= error.stack || error.message || 'No error details available' %>
            </pre>
        </div>
        <% } %>
    </div>

    <script>
        // Create circuit lines
        const circuit = document.getElementById('circuit');
        const lineCount = 20;

        for (let i = 0; i < lineCount; i++) {
            const line = document.createElement('div');
            line.className = 'circuit-line';
            
            if (Math.random() > 0.5) {
                // Horizontal line
                line.style.width = Math.random() * 200 + 50 + 'px';
                line.style.height = '2px';
                line.style.top = Math.random() * 100 + '%';
                line.style.left = Math.random() * 100 + '%';
            } else {
                // Vertical line
                line.style.width = '2px';
                line.style.height = Math.random() * 200 + 50 + 'px';
                line.style.top = Math.random() * 100 + '%';
                line.style.left = Math.random() * 100 + '%';
            }
            
            line.style.animationDelay = Math.random() * 3 + 's';
            circuit.appendChild(line);
        }

        // Auto reload after 30 seconds
        let countdown = 30;
        const countdownInterval = setInterval(() => {
            countdown--;
            if (countdown <= 0) {
                clearInterval(countdownInterval);
                location.reload();
            }
        }, 1000);

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            if (e.key === 'r' || e.key === 'R') {
                location.reload();
            }
            if (e.key === 'h' || e.key === 'H') {
                window.location.href = '/admin/dashboard';
            }
        });
    </script>
</body>
</html>
